function TopBrandItem({ image }) {
  return (
    <div className="w-full h-full">
      <img className="w-full h-full object-contain" src={image} alt="" />
    </div>
  );
}

export default TopBrandItem;
